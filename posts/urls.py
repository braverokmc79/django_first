from django.urls import path
from . import views


app_name = "posts"  # 네임스페이스 지정
  
  
urlpatterns = [
    
    #CRUD URLs
    #  path("리스트/", 리스트클래스.as_view(), name="리스트"),
    #  path("생성/", 생성클래스.as_view(), name="생성"),     
    #  path("상세/<int:pk>/", 상세클래스.as_view(), name="상세"),
    #  path("수정/<int:pk>/", 수정클래스.as_view(), name="수정"),
    #  path("상제/<int:pk>/", 상제클래스.as_view(), name="상제"),
    
]    